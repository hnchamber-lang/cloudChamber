# Chamber 폴더 재편 & 프로젝트 빌더 — 빠른 시작

## 한 줄 요약
`D:\Chamber\_reorg\` 폴더의 `.bat` 파일을 더블클릭하면 모든 작업이 진행됩니다.

---

## 사전 준비 (한 번만)

### 1. Python 설치 확인
명령 프롬프트(`cmd`)에서:
```
python --version
```
3.10 이상이 나오면 OK. 없으면 https://www.python.org/ 에서 설치 후 **"Add Python to PATH" 체크**.

### 2. 외장 디스크 백업 (강력 권장)
```
robocopy D:\Chamber  E:\Chamber_backup_20260424  /MIR /XJ
```

---

## 진행 절차 (순서대로)

### ① 인벤토리 확인 — `run_preflight.bat` 더블클릭
- 현재 D:\Chamber 의 모든 폴더를 스캔
- 각 폴더가 재편 후 어디로 가는지, 파일 수·용량·날짜 범위 출력
- `preflight_report.csv` 생성 → Excel로 열어 확인

### ② 날짜 파싱 검증 — `python validator.py`
- 카탈로그의 날짜 추출 규칙이 실제 파일에 동작하는지 확인
- "All instruments parse dates correctly." 가 떠야 안전
- 문제 발견 시 `python validator.py --verbose` 로 미파싱 파일 예시 확인

### ③ 재편 계획 검토 — `run_dryrun.bat` 더블클릭
- `rename_plan.csv` 생성 (실제 변경 없음)
- Excel로 열어 36단계 이름변경/병합 계획 검토

### ④ 실제 재편 실행 — `run_reorg.bat` 더블클릭
- 콘솔에서 `yes` 입력하여 진행
- 자동으로 `D:\Chamber\_archive_pre_reorg\` 에 원본 백업
- 결과:
  ```
  D:\Chamber\
   ├─ 00_tools\           ← (이전: etc)
   ├─ 00_resources\       ← (이전: files)
   ├─ 01_instruments\     ← 장비별 폴더 (모델_SN시리얼)
   ├─ 02_archives\        ← (이전: POST, thai_old_*)
   ├─ 02_campaigns_legacy\← (이전: YSU, PNU, unist)
   └─ 03_projects\        ← (다음 단계에서 생성됨)
  ```

### ⑤ 미확인 시리얼 채우기 (선택)
1. `serials_to_fill.md` 열기 — 9개 미확인 장비 체크리스트
2. 기기 라벨 확인 후 시리얼 기록
3. `instruments.yaml` 편집:
   - `serial: "<채운값>"`
   - `code:  "<MODEL>_SN<채운값>"`
   - `path:  "01_instruments/<MODEL>_SN<채운값>"`
4. `run_update_serials.bat` 더블클릭 — 폴더명이 자동 변경됨

### ⑥ GUI로 첫 프로젝트 생성 — `run_gui.bat` 더블클릭

**GUI 사용법:**

1. **Project 영역**: 프로젝트명, 제목, PI, 실험자, 시작/종료일, 관측지점, 비고 입력
2. **"Apply to all instruments"** 클릭 → 모든 장비 사용 기간을 프로젝트 기간으로 설정
3. **Instruments 표**:
   - "Use" 컬럼 체크 → 사용한 장비만 선택
   - 장비별로 사용 기간이 다르면 Start/End 직접 수정
4. **Options**:
   - `hardlink` (기본): 디스크 절약 (같은 드라이브일 때)
   - `copy`: 완전 복사 (가장 안전, 용량 큼)
   - `symlink`: 심볼릭 링크 (Windows 개발자 모드 필요)
5. **Save namelist…** : 설정 저장 (재사용 가능)
6. **Preview (dry-run)** : 실제 복사 없이 파일 수만 확인
7. **Build project ▶** : 프로젝트 생성

**결과:**
```
D:\Chamber\03_projects\<프로젝트명>\
 ├─ namelist.yaml           ← 사용한 설정
 ├─ README.md               ← 자동 생성된 요약
 ├─ _log\
 │   ├─ copy_manifest.csv   ← 복사된 파일 목록 (해시 포함)
 │   └─ run.log             ← 빌드 로그
 ├─ CCN200_SN2310-057\      ← 장비별 자료 (날짜 필터링됨)
 ├─ CPC3750_SN3750221105\
 └─ ...
```

---

## 문제 발생 시

### 재편을 되돌리고 싶을 때
```
run_rollback.bat
```
→ `_archive_pre_reorg\` 의 원본을 복원

### GUI가 안 뜰 때
명령 프롬프트에서:
```
cd D:\Chamber\_reorg
python -m pip install pyyaml PyQt5
python chamber_gui.py
```

### 프로젝트를 잘못 만들었을 때
```
python build_project.py <namelist.yaml> --rollback
```
→ 해당 프로젝트 폴더만 삭제

### hardlink가 copy로 떨어질 때
- 원본과 프로젝트가 다른 드라이브면 hardlink 불가
- `copy` 모드 사용 또는 같은 드라이브로 이동

---

## 파일 역할 한눈에 보기

| 파일/폴더 | 용도 |
|---|---|
| `instruments.yaml` | **장비 카탈로그** (시리얼·경로·날짜규칙) |
| `serials_to_fill.md` | 미확인 SN 체크리스트 |
| `사용설명서.md` | 이 문서 |
| `README.md` | 영문 매뉴얼 |
| `run_preflight.bat` | ① 인벤토리 |
| `run_dryrun.bat` | ③ 재편 계획 |
| `run_reorg.bat` | ④ 실제 재편 |
| `run_update_serials.bat` | ⑤ SN 동기화 |
| `run_gui.bat` | ⑥ GUI 실행 |
| `run_rollback.bat` | 비상 시 원복 |
| `validator.py` | 날짜 규칙 검증 |
| `test_e2e.py` | 합성 데이터 전체 테스트 |
| `preflight_report.csv` | 인벤토리 결과 |
| `rename_plan.csv` | 재편 계획표 |

---

## 명령행 사용 (고급)

```bash
cd D:\Chamber\_reorg

# 카탈로그 검증
python validator.py
python validator.py --verbose                     # 미파싱 샘플 표시
python validator.py --code CPI_SN                 # 특정 장비만

# 재편
python reorganize.py --dry-run
python reorganize.py --execute
python reorganize.py --execute --no-archive       # 백업 생략(빠름)
python reorganize.py --update-serials
python reorganize.py --rollback

# 프로젝트 빌더
python build_project.py namelist.MYPROJECT.yaml --preview
python build_project.py namelist.MYPROJECT.yaml
python build_project.py namelist.MYPROJECT.yaml --rollback

# 인벤토리 / 검증
python preflight.py
python test_e2e.py                                # 합성 데이터 회귀 테스트
```

---

## 안전 원칙

1. **`reorganize.py --execute` 는 항상 원본을 `_archive_pre_reorg\` 에 복사 먼저 한 후 변경**합니다.
2. **`build_project.py` 는 `01_instruments\` 를 절대 수정하지 않습니다** — 읽기만 하고, 결과는 `03_projects\` 에 새로 만듭니다.
3. **모든 작업이 로그됨** — `_reorg/run.log`, `01_instruments/_meta/merge_log.csv`, `03_projects/<proj>/_log/copy_manifest.csv` 에 기록.
4. **충돌 감지** — 병합 시 같은 파일명에 다른 내용이면 `_conflicts/` 서브폴더에 보존.
